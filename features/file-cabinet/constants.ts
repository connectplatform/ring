/** Tunnel personal channel for desktop icon layout sync (publishToUserTunnel). */
export const FILE_CABINET_DESKTOP_CHANNEL = 'file-cabinet:desktop-icons'

/** Max nested folder depth (root/a/b/c). Files may live inside the deepest folder. */
export const MAX_FOLDER_DEPTH = 3

/** Same-origin download route (ACL-gated byte stream). */
export const FILE_CABINET_DOWNLOAD_PATH = '/api/file-cabinet/download'
