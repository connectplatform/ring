/** PostgreSQL collection names for public pool tables (schema.sql v4.0.3). */
export const PUBLIC_POOL_COLLECTIONS = {
  pools: 'public_pools',
  signals: 'public_pool_signals',
  contributions: 'public_pool_contributions',
} as const
