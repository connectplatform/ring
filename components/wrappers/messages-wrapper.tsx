'use client'

/**
 * Messages wrapper — thin facade.
 * Layout + inbox orchestration live in MessagesShell (right-rail roster SSOT).
 */
export { default } from '@/features/messages/components/messages-shell'
