---
title: Міграції бази даних
description: PostgreSQL schema та kingdom-міграції для новин і платежів
last_modified: "2026-06-10"
---

# Міграції бази даних

Для `DB_BACKEND_MODE=k8s-postgres-fcm` або `supabase-fcm` застосуйте міграції після `data/schema.sql`.

## Порядок

```bash
psql "$DATABASE_URL" -f data/schema.sql
psql "$DATABASE_URL" -f data/migrations/002_news_content_schema.sql
psql "$DATABASE_URL" -f data/migrations/003_news_kingdom_upgrade.sql
psql "$DATABASE_URL" -f data/migrations/004_payment_transactions.sql
psql "$DATABASE_URL" -f data/migrations/005_refcodes_schema.sql
psql "$DATABASE_URL" -f data/migrations/009_email_crm_jsonb.sql
psql "$DATABASE_URL" -f data/migrations/010_email_crm_tasks_jsonb.sql
```

> **EN canonical:** повний порядок, legacy `001` callout і verify — [Migrations (EN)](/docs/getting-started/migrations)

Допоміжник: `./scripts/run-migration.sh`

Перевірка Email CRM: `psql "$DATABASE_URL" -c "\dt email_*"`

## Опційні seeds

```bash
psql "$DATABASE_URL" -f scripts/seed-news-v1.6.0-uk.sql
```

Див. [Режими backend](/docs/architecture/backend-modes-and-databases) · [Email AI-CRM](/docs/features/email-ai-crm)
