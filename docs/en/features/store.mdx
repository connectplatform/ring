---
title: Multi-Vendor Store
description: Multi-vendor marketplace with vendor onboarding, product CRUD, Agent Knowledge Research (WebConductor), commission settlements, and audience-gated visibility
last_modified: "2026-08-03"
audience: both
---

# Multi-Vendor Store

Ring Platform ships a multi-vendor marketplace where any **verified Entity** can operate a store, list products, and earn through automated commission settlements. Vendors enrich catalog copy with **Agent Knowledge Research** — WebConductor web research into structured fields, `productAgent` markdown, Admin Wiki NODUS, and File Cabinet alt images — then opt media into the product gallery explicitly.

<Callout type="info">
  Use the **Founder** / **Developer** tabs in the docs sidebar to filter this page by audience. Pipeline names live in `scripts/PIPELINES.md` (`store.web_conductor.research_populate`).
</Callout>

<Audience for="founder">

## Why the multi-vendor store matters for your clone

Every Entity in your Ring deployment can become a selling vendor — no external marketplace needed. The store generates platform revenue through **tier-based commission rates** (NEW → BASIC → VERIFIED → TRUSTED → PREMIUM, descending from 20% to 12%) on every sale. Vendors catalogue themselves; operators review submissions and settle commissions.

### Key operator capabilities

<Cards>
  <Card title="Vendor lifecycle" href="/docs/features/store" icon="store">
    Onboard entities as vendors at /vendor/start. Trust scoring, tier progression, and suspension workflows are automated.
  </Card>
  <Card title="Agent Knowledge Research" href="/docs/features/generative-media" icon="sparkles">
    On create/edit product forms: Research → fields + markdown + wiki NODUS + cabinet alt images. **Use in gallery** is explicit — nothing silently replaces the primary photo.
  </Card>
  <Card title="Commission settlements" href="/docs/features/erp/commissions" icon="wallet">
    Tier-based commissions per sale. Admin dashboard at /admin/store/commissions shows pending payouts with referral breakdowns.
  </Card>
  <Card title="Product audience gating" href="/docs/features/store" icon="shield">
    Mark products member-only. Non-members see public products only (`hasMemberPrivileges()`).
  </Card>
</Cards>

### Typical scenarios

- **Local business network** — every shop lists inventory; settlements run automatically.
- **DAO marketplace** — member-gated courses/NFTs; public SKUs as lead magnets.
- **Catalog enrichment** — vendor pastes a manufacturer URL, runs **Research**, reviews suggested fields, saves Agent Knowledge, then clicks **Use in gallery** only for photos they want on the storefront.
- **DAGI assist** — vendor ERP chat tool `dagi_research_product` returns the same suggested fields / research media / cabinet path for the bound vendor.

<Callout type="tip">
  Storefront **product agent** chats use `Conversation.type: 'product'` and stay visible in Messages. **Generative Gallery** tool chats reuse the product channel with `kind: 'generative_gallery'` and `hiddenFromInbox: true` — see [Messaging](/docs/features/messaging) and [Generative Gallery](/docs/features/generative-media).
</Callout>

</Audience>

<Audience for="developer">

## Implementation

### Multi-vendor architecture

<Mermaid>
{`flowchart TB
    subgraph Vendor[Vendor Onboarding]
        Start[/vendor/start]
        Approve[Admin approval]
        Profile[createVendorProfile]
    end

    subgraph Research[Agent Knowledge]
        UI[ProductAgentKnowledgeSection]
        Act[researchProductAgentAction / Draft]
        WC[WebConductor]
        Cab[product-cabinet-media]
        Wiki[createProductNodusWikiFromDraft]
    end

    subgraph Catalog[Catalog SSOT]
        Cache[getCachedProductCatalog]
        API[GET /api/store/products]
        Action[getStoreProducts]
    end

    Vendor --> UI
    UI --> Act --> WC
    Act --> Cab
    Act --> Wiki
    UI --> Cache
    Cache --> API
    Cache --> Action`}
</Mermaid>

### Agent Knowledge Research (Phase W — shipped)

| Piece | Path | Role |
|-------|------|------|
| Conductor | `lib/web/conductor/web-conductor.ts` | TextConductor `webSearch: true` → structured fields + `productAgentMarkdown` + NODUS JSON + citation-host image candidates |
| Orchestrator | `features/store/lib/product-agent-research.ts` | Builds request + `runProductAgentResearch` |
| Cabinet writer | `features/store/lib/product-cabinet-media.ts` | Dirs `store/[storename]/[product-slug]/alt` (fits `MAX_FOLDER_DEPTH` 3; conceptual `files/` prefix), `research-{runId}.md`, SSRF-safe image fetch → RingBase → cabinet nodes |
| Actions | `app/_actions/product-agent-research.ts` | `researchProductAgentAction`, `researchProductDraftAction`, `saveProductAgentKnowledgeAction` |
| UI | `features/store/components/product-agent-knowledge-section.tsx` | Create + edit; **Use in gallery** explicit |
| Wiki helper | `features/store/lib/product-nodus-wiki.ts` | `createProductNodusWikiFromDraft` → tenant Admin Wiki |
| DAGI | `features/store/services/dagi-erp-tools.ts` | `dagi_research_product` → `suggestedFields` / `researchMedia` / `cabinetPath` / `skippedImages` |
| Schema notes | `AI-LEGIOX/legiox-truth-lens/AGENT-PRODUCT-SCHEMA.md` | § photo / research generator steps |
| Pipeline | `scripts/PIPELINES.md` | `store.web_conductor.research_populate` |

**Auth:** vendor entity owner or platform admin (`resolveResearchVendor` + ownership check). Draft research (no product id yet) uses `researchProductDraftAction` and still writes cabinet artifacts under the vendor owner.

**Gallery rule:** research media is never auto-merged as primary. Operators must click **Use in gallery**.

### Product catalog SSOT

`getCachedProductCatalog()` in `features/store/config.ts` (`'use cache'` + `cacheTag('store:products')`). Consumers: `app/api/store/products/route.ts`, `getStoreProducts` in `app/_actions/store-products.ts`, MCP `app/api/mcp/v1/store/products/route.ts`. Invalidate with `updateTag('store:products')` after create.

### Deferred client fetch

`features/store/context.tsx` mounts `StoreProvider` globally but defers the catalog network call until pathname includes `/store` or cart items exist in `localStorage` (single-flight fetch).

### Verified routes

| Route | Purpose | Auth |
|-------|---------|------|
| `/vendor/start` | Vendor onboarding | Subscriber+ |
| `/vendor/products` | Vendor product CRUD + Agent Knowledge | Vendor entity owner |
| `/admin/store/products` | Product approval queue (same form) | Platform admin |
| `/admin/store/commissions` | Commission settlements | Platform admin |
| `/store` | Public storefront + audience filter | Public |

### Product custom fields

<Steps>

<Step>

Vendor selects a category on the product form.

</Step>

<Step>

Adds name+value parameters (“Add product parameter”).

</Step>

<Step>

Persists via `createProductCustomField()` in `app/_actions/vendor-actions.ts` (ownership-guarded).

</Step>

</Steps>

### Product audience filtering

Products with `productAudience: 'member'` are hidden unless `isAdmin || hasMemberPrivileges(currentRole)` in `app/_actions/store-products.ts`.

### Module summary

| Module | Purpose |
|--------|---------|
| `features/store/config.ts` | Adapter + cached catalog SSOT |
| `features/store/context.tsx` | Deferred client catalog + cart |
| `features/store/services/vendor-lifecycle.ts` | Vendor profile + tiers |
| `features/store/services/settlement.ts` | Commission calculation |
| `lib/web/conductor/web-conductor.ts` | Web product research |
| `features/store/lib/product-cabinet-media.ts` | Research → File Cabinet alt |
| `app/_actions/product-agent-research.ts` | Research / draft / save knowledge |
| `app/_actions/vendor-actions.ts` | Vendor product CRUD + research form fields |
| `app/_actions/admin-store-erp.ts` | Admin product path + Agent Knowledge hidden fields |
| `features/auth/user-role.ts` | `hasMemberPrivileges()` |

</Audience>

## Related documentation

<RelatedDocs>
  <RelatedArticle
    slug="features/file-cabinet"
    relation="Same-workflow: research images land under store/product/alt; Image/Video chat enhances siblings."
  />
  <RelatedArticle
    slug="features/generative-media"
    relation="Next-step: Generative Gallery Upload \| Generate on product forms and cabinet scope."
  />
  <RelatedArticle
    slug="features/admin-wiki"
    relation="Depends-on: Research writes product NODUS pages into the tenant wiki vault."
  />
  <RelatedArticle
    slug="features/erp"
    relation="Next-step: ERP hub for stock invariant, commitSale, and settlements cockpit."
  />
  <RelatedArticle
    slug="features/erp/inventory"
    relation="Deep-dive: reserve → commitSaleForOrder → cancel/refund restore."
  />
  <RelatedArticle
    slug="features/payment-conductor"
    relation="Depends-on: PaymentConductor paid handlers drive stock commit."
  />
  <RelatedArticle
    slug="api/store"
    relation="Deep-dive: store REST routes for products, orders, checkout."
  />
</RelatedDocs>

<FutureFeatureBacklog title="Backlog — Store Agent Commerce (not shipped)">

<FutureFeatureWidget
  name="Phase 4 UX polish"
  description="Further Agent Knowledge / Research modal polish beyond the shipped create+edit section and Use in gallery control."
  implementationCost={24}
  labels={['store', 'agent-commerce', 'ux']}
/>

<FutureFeatureWidget
  name="Phase 5 sellable gate"
  description="Hard gate that blocks sellable/publish until research artifacts or gallery completeness meet clone policy — not enforced today."
  implementationCost={32}
  labels={['store', 'agent-commerce', 'compliance']}
/>

<FutureFeatureWidget
  name="Phase 6 Reggie propagate"
  description="Propagate WebConductor Research + cabinet alt wiring to white-label clones via Reggie — platform tree only for now."
  implementationCost={40}
  labels={['store', 'reggie', 'propagation']}
/>

<FutureFeatureWidget
  name="OpenAI tool-loop adapter"
  description="Alternate LLM tool-loop adapter for product research (beyond TextConductor webSearch path)."
  implementationCost={48}
  labels={['store', 'ai', 'conductor']}
/>

<FutureFeatureWidget
  name="Auto-merge research media"
  description="Automatically push research images into the product gallery without explicit Use in gallery — intentionally not shipped."
  implementationCost={16}
  labels={['store', 'gallery']}
/>

<FutureFeatureWidget
  name="Nightly ProcessConductor enrichment"
  description="Scheduled enrichment of stale products via ProcessConductor (TODO in researchProductAgentAction)."
  implementationCost={56}
  labels={['store', 'process-conductor', 'cron']}
/>

<FutureFeatureWidget
  name="On-approve research trigger"
  description="Optional auto-run of WebConductor Research when a product is approved (TODO in researchProductAgentAction)."
  implementationCost={20}
  labels={['store', 'admin', 'agent-commerce']}
/>

</FutureFeatureBacklog>
