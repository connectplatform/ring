---
title: Prerequisites
description: Verified system, Node, PostgreSQL, and environment requirements before installing a Ring Platform clone
last_modified: "2026-06-22"
audience: both
---

# Prerequisites

<Callout type="info">
  Filter with **Founder** / **Developer** in the docs sidebar. This page lists what you need **before** [Installation](/docs/getting-started/installation). Versions match `INSTALL.md`, `install.sh`, and `env.local.template` — not legacy Node 18 guidance.
</Callout>

Ring Platform v1.6+ is a **Next.js 16 + React 19** app with a **mandatory** `DB_BACKEND_MODE`. Production-parity local dev uses **PostgreSQL-primary** (`k8s-postgres-fcm`); Firestore-only (`firebase-full`) is a narrower prototype path.

## At a glance

| Requirement | Verified baseline | Notes |
|-------------|-------------------|--------|
| **Node.js** | **v20+** (`install.sh` check; v22+ recommended in `INSTALL.md`) | Custom dev server: `node --import tsx server.ts` |
| **npm** | **10+** | `pnpm` / `yarn` also work per installation guide |
| **Git** | Any recent version | Clone [connectplatform/ring](https://github.com/connectplatform/ring) |
| **PostgreSQL** | **14+** (16 Docker image; **18** recommended for Homebrew dev) | DB `ring_platform`, user `ring_user` |
| **PostGIS** | Optional | Location-heavy clones; safe to skip initially |
| **Disk / RAM** | ~4 GB RAM, ~10 GB free disk for dev | Full monorepo + `node_modules` |
| **Ports** | `3000` (app), `5432` (Postgres) | Native WSS on same host via `server.ts` when self-hosted |

<Audience for="founder">

## What founders need before ringizing

You are preparing a **white-label clone** — branding in `ring-config.json`, secrets in `.env.local`, and (for production features) a Postgres database with schema applied.

### MVP vs production-ready

<Cards>
  <Card title="Explore UI locally" href="/docs/getting-started/installation">
    Node 20+, `install.sh --quick`, Postgres **or** short-lived `firebase-full` prototype — see [Database selection](/docs/customization/database-selection).
  </Card>
  <Card title="Production-parity clone" href="/docs/getting-started/migrations">
    **Postgres-primary** + `data/schema.sql` v4+ + kingdom migrations — required for store, PaymentConductor, News Kingdom depth.
  </Card>
  <Card title="Sign-in for testers" href="/docs/features/authentication">
    At least one Auth.js path: Google OAuth, Apple, or Ring Mailer (`SMTP_*` or `EMAIL_MODE=ethereal`).
  </Card>
  <Card title="Payments later" href="/docs/features/payment-conductor">
    WayForPay / Stripe credentials — **not** required for first `npm run dev`; add before checkout.
  </Card>
</Cards>

### Honest time budget

| Path | Time | Outcome |
|------|------|---------|
| `./install.sh --quick` + existing Postgres | ~20–40 min | Env scaffold; you still apply schema + OAuth |
| Full Postgres-primary bootstrap | 45–90 min | Matches ring-platform.org dev stack |
| `firebase-full` prototype | ~15–30 min | Skips ERP / conductor depth |

<Callout type="warning">
  **Ringdom-managed hosting** skips most of this checklist — settlers receive a configured cluster. Self-hosters on GitHub follow this section ([OSS vs enterprise](/docs/development/oss-vs-enterprise)).
</Callout>

</Audience>

<Audience for="developer">

## Toolchain

### Node.js and package manager

<Code language="bash" title="Verify versions">{`node --version   # v20.0.0+ (v22+ recommended)
npm --version    # 10+`}</Code>

`install.sh` fails early if Node is below v20. Use nvm, fnm, or official installers — see `INSTALL.md`.

### PostgreSQL

Default connection (from `env.local.template`):

<Code language="bash" title=".env.local excerpt">{`DB_BACKEND_MODE=k8s-postgres-fcm
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ring_platform
DB_USER=ring_user
DB_PASSWORD=ring_password_2024
# or: DATABASE_URL=postgresql://ring_user:ring_password_2024@localhost:5432/ring_platform`}</Code>

Quick Docker dev database (matches [Installation](/docs/getting-started/installation)):

<Code language="bash" title="postgres:16-alpine">{`docker run -d --name ring-postgres-dev \\
  -e POSTGRES_USER=ring_user \\
  -e POSTGRES_PASSWORD=ring_password_2024 \\
  -e POSTGRES_DB=ring_platform \\
  -p 5432:5432 \\
  postgres:16-alpine`}</Code>

Apply `data/schema.sql` then kingdom migrations — [Database migrations](/docs/getting-started/migrations).

### Backend mode (required)

Set **`DB_BACKEND_MODE`** before first boot:

| Value | When |
|-------|------|
| `k8s-postgres-fcm` | **Default** — Postgres app data + optional FCM push |
| `supabase-fcm` | Postgres on Supabase |
| `firebase-full` | Firestore prototype only |

See [Backend modes](/docs/architecture/backend-modes-and-databases). **`DATABASE_MODE` is removed** — do not document or set it.

### Minimum environment variables

<Code language="bash" title="First boot minimum">{`cp env.local.template .env.local
# install.sh generates AUTH_SECRET and WALLET_ENCRYPTION_KEY

AUTH_SECRET=...                    # required — npx @better-auth/cli secret
DB_BACKEND_MODE=k8s-postgres-fcm
DB_* or DATABASE_URL               # required for postgres modes

# At least one sign-in path:
AUTH_GOOGLE_ID / AUTH_GOOGLE_SECRET
# and/or AUTH_APPLE_*
# and/or Ring Mailer: EMAIL_MODE=ethereal or SMTP_HOST + SMTP_USER + SMTP_PASSWORD
# (migration 038_email_login_tokens.sql for OTP / magic link)`}</Code>

Providers registered in `auth.ts`: **Ring Mailer** Credentials (`email-otp`, `email-magic`, `credentials`), **Google** OAuth, **Google One Tap**, **Apple**, wallet/crypto credentials. GitHub/Discord OAuth are **not** in the default provider list. **Resend / `AUTH_RESEND_KEY` removed.**

Optional for later features:

- `BLOB_READ_WRITE_TOKEN` — Vercel Blob uploads  
- `WAYFORPAY_*` / `STRIPE_*` — PaymentConductor  
- `NEXT_PUBLIC_FIREBASE_*` + service account — FCM push (not Firestore DB on postgres-primary)  
- `OPENAI_API_KEY` / `ANTHROPIC_API_KEY` — AI matcher / Email CRM  

Never commit `.env.local`. See [Environment configuration](/docs/deployment/environment).

### Validation checklist

<Steps>
<Step>

### System

<Code language="bash" title="terminal">{`node --version && npm --version
curl -fsS -o /dev/null https://registry.npmjs.org/ || echo "npm registry unreachable"`}</Code>

</Step>
<Step>

### Database

<Code language="bash" title="terminal">{`psql "$DATABASE_URL" -c "SELECT version();"
# or:
docker exec ring-postgres-dev psql -U ring_user -d ring_platform -c "SELECT 1;"`}</Code>

</Step>
<Step>

### Health (after install)

<Code language="bash" title="after npm run dev">{`curl -sS http://localhost:3000/api/health | jq .status`}</Code>

Expect `"healthy"` when `AUTH_SECRET` is set. See [Monitoring](/docs/deployment/monitoring).

</Step>
</Steps>

### Optional: Docker-only stack

Docker Compose can run the app + Redis; Postgres is often a separate container or host service. Monitoring profile adds Prometheus/Grafana — see caveats in [Monitoring](/docs/deployment/monitoring).

</Audience>

## Next steps

<Cards>
  <Card title="Installation" href="/docs/getting-started/installation">
    `install.sh`, schema apply, `npm run dev`
  </Card>
  <Card title="First success" href="/docs/getting-started/first-success">
    Smoke-test checklist after boot
  </Card>
  <Card title="Troubleshooting" href="/docs/getting-started/troubleshooting">
    Common install failures
  </Card>
</Cards>

<Callout type="success">
  Prerequisites met? Continue to [Installation](/docs/getting-started/installation).
</Callout>

<FutureFeatureBacklog title="Backlog — prerequisites (TBD)">

<FutureFeatureWidget
  name="GitHub OAuth as default provider"
  description="Pre-wired GitHub sign-in in env.local.template — legacy doc listed it as required; clone uses Google/Apple/email per ring-config."
  implementationCost={8}
  labels={['auth', 'onboarding']}
  voteCount={0}
/>

<FutureFeatureWidget
  name="Discord community gate"
  description="Mandatory Discord join before first login — removed fictional onboarding requirement."
  implementationCost={16}
  labels={['community', 'onboarding']}
  voteCount={0}
/>

<FutureFeatureWidget
  name="Node 18 LTS pin"
  description="Document and CI matrix for Node 18 — platform baseline is Node 20+ (engines in package.json)."
  implementationCost={4}
  labels={['tooling']}
  voteCount={0}
/>

</FutureFeatureBacklog>
