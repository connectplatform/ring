---
title: Ring CLI
description: Полное руководство по интерфейсу командной строки Ring Platform для развертывания и управления
---

# Ring Platform CLI

**Ring CLI** - это мощный интерфейс командной строки, который упрощает развертывание, настройку и управление приложениями Ring Platform. С помощью простых команд вы можете развертывать в продакшн, управлять конфигурациями и отслеживать состояние вашей платформы.

## 🚀 Быстрый старт

### Установка

Установите Ring CLI глобально из вашей директории Ring Platform:

Перейдите в директорию Ring Platform Установите глобально с помощью npm link Проверьте установку Вывод: 1.0.1

<Code language="bash" title="terminal">
{`cd ring-platform.org

npm link

ring --version`}
</Code>

### Первое развертывание

Разверните в продакшн одной командой:

Развертывание с версией из package.json Развертывание конкретной версии Предпросмотр без выполнения

<Code language="bash" title="terminal">
{`ring prod

ring prod --version 0.9.22

ring prod --dry-run`}
</Code>

## 🎯 Основные команды

### `ring prod` - Развертывание в продакшн

Развертывание Ring Platform в продакшн кластер Kubernetes с автоматической сборкой Docker, отправкой и развертыванием.

Развертывание с автоопределением версии Развертывание конкретной версии Пропуск определенных шагов Предпросмотр команд без выполнения

<Code language="bash" title="terminal">
{`ring prod

ring prod --version 0.9.22

ring prod --skip-build    # Использовать существующий Docker образ
ring prod --skip-push     # Не отправлять в реестр
ring prod --skip-deploy   # Только сборка и отправка

ring prod --dry-run`}
</Code>

### `ring config` - Управление конфигурацией

Управление глобальной конфигурацией CLI, хранящейся в `~/.ring-platform/config.json`.

Установка значений Чтение конфигурации Сброс к значениям по умолчанию

<Code language="bash" title="terminal">
{`ring config --set k8s.controlNode=my-k8s-server
ring config --set database.type=postgresql

ring config --get database.type
ring config --list

ring config --reset`}
</Code>

### `ring status` - Статус развертывания

Проверка состояния и статуса вашего развертывания Ring Platform.

Базовая проверка статуса Детальный вывод Проверка другого пространства имен

<Code language="bash" title="terminal">
{`ring status

ring status --verbose

ring status --namespace staging`}
</Code>

### `ring test` - Тестирование базы данных и сервисов

Запуск комплексных тестов для соединения с базой данных, сервисов и целостности данных.

Тестирование соединения с PostgreSQL Тестирование инициализации DatabaseService Тестирование получения данных пользователей Тестирование функциональности запросов имени пользователя

<Code language="bash" title="terminal">
{`ring test --type db-connection

ring test --type db-service

ring test --type user-data

ring test --type username`}
</Code>

#### Типы тестов

**`db-connection`** - Тестирует соединение с PostgreSQL и базовое состояние базы данных:
- Проверяет соединение с сервером PostgreSQL
- Проверяет наличие таблицы пользователей
- Сообщает количество пользователей и состояние базы данных

**`db-service`** - Тестирует инициализацию DatabaseService и операции:
- Инициализирует слой абстракции базы данных
- Тестирует операции чтения из таблицы пользователей
- Валидирует конфигурацию сервиса и обработку ошибок

**`user-data`** - Тестирует получение данных пользователей и валидность структуры:
- Запрашивает конкретные данные пользователя по ID
- Валидирует структуру данных JSONB
- Проверяет информацию о кошельках, ролях и профиле

**`username`** - Тестирует функциональность запросов имени пользователя и структуру данных:
- Тестирует запросы на основе имени пользователя
- Валидирует синтаксис запросов JSONB
- Проверяет целостность структуры данных пользователей

#### Пример вывода

```
🧪 Запуск тестов Ring Platform...

Тестирование соединения с PostgreSQL...
✅ Соединение с PostgreSQL успешно!
Таблица пользователей существует: ✅
Количество пользователей: 1

✅ Тест завершен успешно!
```

## ⚙️ Структура конфигурации

Ring CLI хранит конфигурацию в `~/.ring-platform/config.json`:

- **k8s** - Настройки Kubernetes (controlNode, namespace, deployment, registry)
- **database** - Конфигурация базы данных (type, host, port, name, user)
- **auth** - Настройки аутентификации (secret, googleId, appleId)
- **web3** - Web3 конфигурация (walletEncryptionKey, polygonRpcUrl)
- **app** - Настройки приложения (url, apiUrl)

## 🔧 Продвинутое использование

### Интеграция CI/CD

Пример для GitHub Actions:

```yaml
on:
  push:
    tags:
      - 'v*'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Установка Ring CLI
        run: npm link
      - name: Развертывание
        run: ring prod --version ${GITHUB_REF#refs/tags/v}
```

## 🐛 Устранение неполадок

### Распространенные проблемы

**Команда не найдена:**
<Code language="bash" title="terminal">
{`cd ring-platform.org
npm link`}
</Code>

**Ошибка SSH подключения:**
<Code language="bash" title="ring config --set k8s.controlNode=your-actual-host">
{``}
</Code>

**Ошибка сборки Docker:**
<Code language="bash" title="terminal">
{`docker login ghcr.io`}
</Code>

## 📚 Связанная документация

- [Руководство по развертыванию](/ru/library/deployment/docker)
- [Настройка разработки](/ru/library/development/local-setup)
- [Руководство по конфигурации](/ru/library/deployment/environment)

---

*Ring CLI постоянно развивается. Проверяйте обновления с помощью `ring --version` и обновляйтесь через `git pull && npm link`.*
