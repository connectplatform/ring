---
title: Мульти-тенант
description: Документація мульти-тенант для платформи Ring
last_modified: 2025-10-16
---

# Мульти-доменне white-label розгортання

Налаштуйте декілька окремих white-label інстансів платформи Ring з різними доменами, брендингом та конфігураціями для різних клієнтів чи ринків.

## 🏢 Підхід до мульти-доменності

Платформа Ring підтримує мульти-доменне розгортання через:
- **Конфігурація за доменом** - Різні налаштування для кожного домену
- **Змінні середовища** - Окремі env файли для кожного інстансу
- **База даних** - Спільна або ізольована залежно від потреб
- **Масштабованість** - Одне розгортання обслуговує декілька брендів

## 🚀 Налаштування мульти-доменності

### Крок 1: Структура каталогів
```
deployments/
├── client1/
│   ├── .env.local
│   ├── brand-assets/
│   └── config.json
├── client2/
│   ├── .env.local
│   ├── brand-assets/
│   └── config.json
└── default/
    ├── .env.local
    └── config.json
```

### Крок 2: Конфігурація доменів
// config/domains.ts

<Code language="typescript" title="TypeScript">
{`export interface DomainConfig {
  domain: string
  brandName: string
  brandTagline: string
  logo: string
  colors: {
    primary: string
    secondary: string
    accent: string
  }
  features: {
    entities: boolean
    opportunities: boolean
    messaging: boolean
    wallet: boolean
    store: boolean
  }
  database: {
    schema: string  // Окрема схема для кожного клієнта
    prefix: string  // Префікс таблиць
  }
}

export const domainConfigs: Record<string, DomainConfig> = {
  'client1.yourplatform.com': {
    domain: 'client1.yourplatform.com',
    brandName: 'Тех Корп',
    brandTagline: 'Інноваційні технологічні рішення',
    logo: '/brands/client1/logo.svg',
    colors: {
      primary: '#3b82f6',
      secondary: '#1e40af',
      accent: '#06b6d4'
    },
    features: {
      entities: true,
      opportunities: true,
      messaging: true,
      wallet: true,
      store: false
    },
    database: {
      schema: 'client1',
      prefix: 'c1_'
    }
  },
  'client2.yourplatform.com': {
    domain: 'client2.yourplatform.com',
    brandName: 'Бізнес Хаб',
    brandTagline: 'З'єднуємо бізнес та можливості',
    logo: '/brands/client2/logo.svg',
    colors: {
      primary: '#10b981',
      secondary: '#059669',
      accent: '#f59e0b'
    },
    features: {
      entities: true,
      opportunities: false,
      messaging: true,
      wallet: false,
      store: true
    },
    database: {
      schema: 'client2',
      prefix: 'c2_'
    }
  }
}`}
</Code>

### Крок 3: Middleware для маршрутизації
// middleware.ts

<Code language="typescript" title="TypeScript">
{`import { NextRequest, NextResponse } from 'next/server'
import { domainConfigs } from './config/domains'

export function middleware(request: NextRequest) {
  const hostname = request.headers.get('host') || ''
  const config = domainConfigs[hostname]

  if (config) {
    // Встановити cookies або headers для ідентифікації домену
    const response = NextResponse.next()
    response.cookies.set('domain-config', JSON.stringify(config))
    return response
  }

  // Дефолтний домен
  return NextResponse.next()
}`}
</Code>

### Крок 4: Провайдер конфігурації домену
// providers/DomainProvider.tsx

<Code language="typescript" title="TypeScript">
{`'use client'

import { createContext, useContext, useEffect, useState } from 'react'

interface DomainContextType {
  config: DomainConfig | null
  isLoading: boolean
}

const DomainContext = createContext<DomainContextType>({
  config: null,
  isLoading: true
})

export function DomainProvider({ children }: { children: React.ReactNode }) {
  const [config, setConfig] = useState<DomainConfig | null>(null)
  const [isLoading, setIsLoading] = useState(true)

  useEffect(() => {
    // Отримати конфігурацію з cookies або API
    const getDomainConfig = async () => {
      try {
        const response = await fetch('/api/domain-config')
        const domainConfig = await response.json()
        setConfig(domainConfig)
      } catch (error) {
        console.error('Failed to load domain config:', error)
      } finally {
        setIsLoading(false)
      }
    }

    getDomainConfig()
  }, [])

  return (
    <DomainContext.Provider value={{ config, isLoading }}>
      {children}
    </DomainContext.Provider>
  )
}

export function useDomain() {
  return useContext(DomainContext)
}`}
</Code>

## 🎨 Брендинг за доменом

### Динамічне завантаження стилів
// lib/branding.ts

<Code language="typescript" title="TypeScript">
{`export function loadDomainBranding(config: DomainConfig) {
  if (typeof document === 'undefined') return

  const root = document.documentElement

  // Встановити CSS змінні
  root.style.setProperty('--brand-primary', config.colors.primary)
  root.style.setProperty('--brand-secondary', config.colors.secondary)
  root.style.setProperty('--brand-accent', config.colors.accent)

  // Оновити title та meta теги
  document.title = config.brandName
  const metaDescription = document.querySelector('meta[name="description"]')
  if (metaDescription) {
    metaDescription.setAttribute('content', config.brandTagline)
  }
}`}
</Code>

### Компонент логотипу домену
// components/DomainLogo.tsx

<Code language="typescript" title="TypeScript">
{`import { useDomain } from '@/providers/DomainProvider'

export function DomainLogo() {
  const { config } = useDomain()

  if (!config) return null

  return (
    <div className="flex items-center space-x-2">
      <img
        src={config.logo}
        alt={config.brandName}
        className="h-8 w-auto"
      />
      <span className="font-bold text-xl">{config.brandName}</span>
    </div>
  )
}`}
</Code>

## 🗄️ Ізоляція даних

### Підхід до ізоляції
<Code language="sql" title="-- Створення окремої схеми для кожного клієнта">
{`CREATE SCHEMA client1;
CREATE SCHEMA client2;

-- Таблиці в межах схеми клієнта
CREATE TABLE client1.entities (
  id SERIAL PRIMARY KEY,
  name VARCHAR(255) NOT NULL,
  -- ... інші поля
);

CREATE TABLE client2.entities (
  id SERIAL PRIMARY KEY,
  name VARCHAR(255) NOT NULL,
  -- ... інші поля
);`}
</Code>

### API з підтримкою мульти-тенантності
// lib/api/multi-tenant.ts

<Code language="typescript" title="TypeScript">
{`export class MultiTenantAPI {
  private getSchema(): string {
    // Отримати схему з контексту домену
    return 'client1' // або з cookies/session
  }

  async getEntities() {
    const schema = this.getSchema()
    const query = `SELECT * FROM ${schema}.entities`

    return this.db.query(query)
  }
}`}
</Code>

## 🚀 Розгортання мульти-доменності

### Nginx конфігурація
/etc/nginx/sites-available/multi-domain

<Code language="nginx" title="configuration">
{`server {
    listen 80;
    server_name client1.yourplatform.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

server {
    listen 80;
    server_name client2.yourplatform.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}`}
</Code>

### Docker Compose для мульти-інстансів
docker-compose.multi-tenant.yml

<Code language="yaml" title="configuration">
{`version: '3.8'

services:
  client1:
    image: ring-platform:latest
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_BRAND_NAME=Client 1
      - DATABASE_SCHEMA=client1
    volumes:
      - ./deployments/client1/.env.local:/app/.env.local

  client2:
    image: ring-platform:latest
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_BRAND_NAME=Client 2
      - DATABASE_SCHEMA=client2
    volumes:
      - ./deployments/client2/.env.local:/app/.env.local`}
</Code>

## 🏷️ Контрольний список мульти-доменності

### Домен та інфраструктура
- [ ] **Домени** зареєстровані та DNS налаштований
- [ ] **SSL сертифікати** отримані для всіх доменів
- [ ] **Nginx/Proxy** конфігурація для маршрутизації
- [ ] **CDN** налаштований для кожного домену

### База даних
- [ ] **Схеми** створені для кожного клієнта
- [ ] **Міграції** виконані для всіх схем
- [ ] **Ізоляція даних** протестована
- [ ] **Бекапи** налаштовані окремо

### Конфігурація бренду
- [ ] **Активи** завантажені для кожного домену
- [ ] **CSS змінні** налаштовані
- [ ] **Мета теги** динамічно оновлюються
- [ ] **PWA маніфест** адаптований

### Функціональність
- [ ] **Функції** увімкнені/вимкнені за доменом
- [ ] **Локалізація** працює для кожного ринку
- [ ] **Інтеграції** протестовані окремо
- [ ] **Моніторинг** налаштований

## 💡 Приклади використання

### SaaS платформа
Кожен клієнт отримує власний white-label додаток з повною ізоляцією даних та кастомним брендингом.

### Регіональні ринки
Різні домени для різних країн з локалізованою версіями та регуляторними вимогами.

### Корпоративні клієнти
Великі організації з декількома підрозділами, кожен з власним брендингом та конфігурацією.

---

**🏢 Масштабованість та гнучкість**

Мульти-доменна архітектура дозволяє обслуговувати декілька клієнтів з однієї кодової бази, забезпечуючи повну ізоляцію та кастомізацію для кожного.

*Потрібна допомога з мульти-доменним розгортанням? Зверніться до нашої [документації з white-label](https://ring-platform.org/docs/white-label) або [контактної форми](https://ring-platform.org/contact).*"

