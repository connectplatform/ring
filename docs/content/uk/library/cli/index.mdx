---
title: Ring CLI
description: Повне керівництво по інтерфейсу командного рядка Ring Platform для розгортання та управління
---

# Ring Platform CLI

**Ring CLI** - це потужний інтерфейс командного рядка, який спрощує розгортання, налаштування та управління додатками Ring Platform. За допомогою простих команд ви можете розгортати в продакшн, керувати конфігураціями та відстежувати стан вашої платформи.

## 🚀 Швидкий старт

### Встановлення

Встановіть Ring CLI глобально з вашої директорії Ring Platform:

Перейдіть до директорії Ring Platform Встановіть глобально за допомогою npm link Перевірте встановлення Вивід: 1.0.1

<Code language="bash" title="terminal">
{`cd ring-platform.org

npm link

ring --version`}
</Code>

### Перше розгортання

Розгорніть в продакшн однією командою:

Розгортання з версією з package.json Розгортання конкретної версії Попередній перегляд без виконання

<Code language="bash" title="terminal">
{`ring prod

ring prod --version 0.9.22

ring prod --dry-run`}
</Code>

## 🎯 Основні команди

### `ring prod` - Розгортання в продакшн

Розгортання Ring Platform в продакшн кластер Kubernetes з автоматичною збіркою Docker, відправкою та розгортанням.

Розгортання з автовизначенням версії Розгортання конкретної версії Пропуск певних кроків Попередній перегляд команд без виконання

<Code language="bash" title="terminal">
{`ring prod

ring prod --version 0.9.22

ring prod --skip-build    # Використовувати існуючий Docker образ
ring prod --skip-push     # Не відправляти до реєстру
ring prod --skip-deploy   # Тільки збірка та відправка

ring prod --dry-run`}
</Code>

### `ring config` - Управління конфігурацією

Управління глобальною конфігурацією CLI, що зберігається в `~/.ring-platform/config.json`.

Встановлення значень Читання конфігурації Скидання до значень за замовчуванням

<Code language="bash" title="terminal">
{`ring config --set k8s.controlNode=my-k8s-server
ring config --set database.type=postgresql

ring config --get database.type
ring config --list

ring config --reset`}
</Code>

### `ring status` - Статус розгортання

Перевірка стану та статусу вашого розгортання Ring Platform.

Базова перевірка статусу Детальний вивід Перевірка іншого простору імен

<Code language="bash" title="terminal">
{`ring status

ring status --verbose

ring status --namespace staging`}
</Code>

### `ring test` - Тестування бази даних та сервісів

Запуск комплексних тестів для з'єднання з базою даних, сервісів та цілісності даних.

Тестування з'єднання з PostgreSQL Тестування ініціалізації DatabaseService Тестування отримання даних користувачів Тестування функціональності запитів імені користувача

<Code language="bash" title="terminal">
{`ring test --type db-connection

ring test --type db-service

ring test --type user-data

ring test --type username`}
</Code>

#### Типи тестів

**`db-connection`** - Тестує з'єднання з PostgreSQL та базовий стан бази даних:
- Перевіряє з'єднання з сервером PostgreSQL
- Перевіряє наявність таблиці користувачів
- Повідомляє кількість користувачів та стан бази даних

**`db-service`** - Тестує ініціалізацію DatabaseService та операції:
- Ініціалізує шар абстракції бази даних
- Тестує операції читання з таблиці користувачів
- Валідує конфігурацію сервісу та обробку помилок

**`user-data`** - Тестує отримання даних користувачів та валідність структури:
- Запитує конкретні дані користувача за ID
- Валідує структуру даних JSONB
- Перевіряє інформацію про гаманці, ролі та профіль

**`username`** - Тестує функціональність запитів імені користувача та структуру даних:
- Тестує запити на основі імені користувача
- Валідує синтаксис запитів JSONB
- Перевіряє цілісність структури даних користувачів

#### Приклад виводу

```
🧪 Запуск тестів Ring Platform...

Тестування з'єднання з PostgreSQL...
✅ З'єднання з PostgreSQL успішне!
Таблиця користувачів існує: ✅
Кількість користувачів: 1

✅ Тест завершено успішно!
```

## ⚙️ Структура конфігурації

Ring CLI зберігає конфігурацію в `~/.ring-platform/config.json`:

- **k8s** - Налаштування Kubernetes (controlNode, namespace, deployment, registry)
- **database** - Конфігурація бази даних (type, host, port, name, user)
- **auth** - Налаштування аутентифікації (secret, googleId, appleId)
- **web3** - Web3 конфігурація (walletEncryptionKey, polygonRpcUrl)
- **app** - Налаштування додатку (url, apiUrl)

## 🔧 Розширене використання

### Інтеграція CI/CD

Приклад для GitHub Actions:

```yaml
on:
  push:
    tags:
      - 'v*'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Встановлення Ring CLI
        run: npm link
      - name: Розгортання
        run: ring prod --version ${GITHUB_REF#refs/tags/v}
```

## 🐛 Усунення несправностей

### Поширені проблеми

**Команда не знайдена:**
<Code language="bash" title="terminal">
{`cd ring-platform.org
npm link`}
</Code>

**Помилка SSH підключення:**
<Code language="bash" title="ring config --set k8s.controlNode=your-actual-host">
{``}
</Code>

**Помилка збірки Docker:**
<Code language="bash" title="terminal">
{`docker login ghcr.io`}
</Code>

## 📚 Пов'язана документація

- [Керівництво з розгортання](/uk/docs/deployment/docker)
- [Налаштування розробки](/uk/docs/development/local-setup)
- [Керівництво з конфігурації](/uk/docs/deployment/environment)

---

*Ring CLI постійно розвивається. Перевіряйте оновлення за допомогою `ring --version` та оновлюйтесь через `git pull && npm link`.*
