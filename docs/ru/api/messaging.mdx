---
title: Messaging
description: Messaging documentation for Ring Platform
---

# Messaging API

Real-time communication system with **8 endpoints** supporting conversations, messages, typing indicators, and file attachments.

## Overview

The Ring Platform messaging system provides:
- Real-time conversation management
- Message CRUD operations
- Typing indicators and presence
- File attachment support
- Cross-platform compatibility

## API Endpoints

### `GET /api/conversations`
List user conversations with pagination

### `POST /api/conversations`
Create new conversation

### `GET /api/conversations/{id}/messages`
Get messages in conversation

### `POST /api/conversations/{id}/messages`
Send message to conversation

### `PUT /api/messages/{id}`
Update message content

### `DELETE /api/messages/{id}`
Delete message

### `POST /api/conversations/{id}/typing`
Send typing indicator

### `PUT /api/messages/{id}/read`
Mark message as read

## Real-time Features

The messaging system uses **Tunnel Transport** abstraction for optimal performance:

- **Native WSS** — primary when `RING_DEPLOY_TARGET` is `k8s` or `self-hosted` (`/api/tunnel/ws`)
- **SSE** — primary on Vercel; fallback after WSS
- **Long-polling** — universal fallback via `/api/tunnel/poll`

---

<Callout type="development" title="Документация в разработке">
Полная документация по сообщениям дополняется. См. раздел [Features](/ru/docs/features/messaging) для текущих деталей реализации.
</Callout>
