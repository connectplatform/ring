'use client'

/**
 * L1 stub — platform store has no niche product fields.
 * L2 packs overwrite this file with vertical forms (e.g. agricultural).
 */
export default function NicheProductFieldsSection() {
  return null
}
