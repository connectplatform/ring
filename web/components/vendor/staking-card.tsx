/**
 * @deprecated Use GateStakeCard from gate-stake-card.tsx.
 * Kept so existing imports of StakingCard resolve to GateEscrow UX
 * (not the old DAARION / NATIVE_NFT_APR mock).
 */

export { GateStakeCard as StakingCard, GateStakeCard } from './gate-stake-card'
