/**
 * Scaffold — accept collaboration invite by token (Zemna Phase 4).
 */

import { NextResponse } from 'next/server'

export async function GET() {
  return NextResponse.json(
    {
      success: false,
      error: 'not_implemented',
      message: 'Invite accept flow scaffolded; UI sprint pending.',
    },
    { status: 501 },
  )
}

export async function POST() {
  return NextResponse.json(
    {
      success: false,
      error: 'not_implemented',
      message: 'Invite accept flow scaffolded; UI sprint pending.',
    },
    { status: 501 },
  )
}
