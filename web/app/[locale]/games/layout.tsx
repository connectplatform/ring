'use client'

/**
 * Games layout — IncomingGameBanner is global (GlobalTunnelListeners).
 */
export default function GamesLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
